export enum {{ enum.name | caseUcfirst  }}s {
{% for value in enum.enum %}
{% set key = enum.keys is empty ? value : enum.keys[loop.index0] %}
    {{ key | replace({'-': ''}) | caseEnumKey }} = '{{ value }}',
{% endfor %}
}

export type {{ enum.name | caseUcfirst  }} = {{ enum.name | caseUcfirst  }}s | `${ {{ enum.name | caseUcfirst  }}s }`;